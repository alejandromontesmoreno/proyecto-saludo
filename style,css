/* Estilo base para el QDialog */
QDialog {
    background-color: #f5f7fa;  /* fondo claro moderno */
    border: 1px solid #dfe3e8;
    border-radius: 10px;
    padding: 10px;
    font-family: "Segoe UI", "Roboto", sans-serif;
    font-size: 14px;
    color: #2c3e50;
}

/* Estilo para QLabel */
QLabel {
    color: #2c3e50;
    font-size: 14px;
}

/* QLineEdit dentro del diálogo */
QLineEdit {
    border: 1px solid #cfd8dc;
    border-radius: 5px;
    padding: 6px 10px;
    background-color: white;
    font-size: 14px;
}
QLineEdit:focus {
    border: 1px solid #42a5f5;
    background-color: #e3f2fd;
}

/* QTextEdit (si lo usas) */
QTextEdit {
    border: 1px solid #cfd8dc;
    border-radius: 5px;
    padding: 6px;
    background-color: white;
    font-size: 14px;
}

/* QComboBox */
QComboBox {
    border: 1px solid #cfd8dc;
    border-radius: 5px;
    padding: 5px;
    background-color: white;

}

/* QPushButton */
QPushButton {
    background-color: #42a5f5;
    color: white;
    border: none;
    border-radius: 5px;
    padding: 8px 14px;
    font-weight: bold;
    font-size: 14px;
font-weight: 300;
}
QPushButton:hover {
    background-color: #1e88e5;
}
QPushButton:pressed {
    background-color: #1565c0;
}

/* QCheckBox */
QCheckBox {
    spacing: 6px;
}
QCheckBox::indicator {
    width: 16px;
    height: 16px;
}
QCheckBox::indicator:unchecked {
    border: 1px solid #aaa;
    background: white;
    border-radius: 3px;
}
QCheckBox::indicator:checked {
    background-color: #42a5f5;
    border: 1px solid #42a5f5;
    border-radius: 3px;
}
